import React from 'react';
import { ToastContainer, toast } from 'react-toastify';

const Contract = () => {
  return (
    <div>
      <h2>What will happen with the contract? <a href="/">Go back to home</a></h2>
    </div>
  );
}

export default Contract;
